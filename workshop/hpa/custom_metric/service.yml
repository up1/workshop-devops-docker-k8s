apiVersion: v1
kind: Service
metadata:
  name: metrics-example
  labels:
    app: metrics-example
spec:
  selector:
    app: metrics-example
  ports:
    - name: http
      port: 80
      protocol: TCP
